// import {Consumer} from './model/Consumer';
// import {ConsumerItem} from './model/ConsumerItem';
import {Driver, ServiceResponse} from 'loca-boot-core';
import {CoreService} from 'loca-boot-service';
// import {VueResourceApi} from 'loca-plugin-vue-resource-api';

describe('ModelBase', () => {

  describe('saveChangedData', () => {
    it('consumerList: ConsumerItem[], push one item to list, then save changed.', async() => {
      // const s = new CoreService(new Driver({
      //   api: new VueResourceApi(),
      //   url: '',
      // }));
      // const res = await s.post<ConsumerItem[]>('url', {
      //   wrapper: [ConsumerItem],
      // });
      // const res = await s.post('url', {
      //   wrapper: {
      //     type: 'DataListWrapper',
      //   },
      // });
    });
  });
});
